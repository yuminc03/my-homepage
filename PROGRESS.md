# 진행 상황
최종 업데이트: 2026-09-13

## 현재 상태 한 줄 요약
디자인 시안(4개 화면)은 **데스크톱 메타포**로 확정되었고, **라이트/다크 모드(바탕만 전환)**, **Dock 자동 숨김**, **G · 라일락 & 민트 색상**까지 시안에 반영했다. 남은 시안 작업은 적용 결과 세부 조정, 모션·반응형 설계이며, 기술 스택은 시안을 마무리한 뒤 정하기로 보류했다. 아직 사이트 코드는 작성하지 않았다.

## 프로젝트 개요
- 자기소개, 프로젝트 포트폴리오, 학습 기록, 세미나·행사 기록(사진 포함)을 모으는 개인 홈페이지
- 만드는 이유: 회사에 소속되지 않은 기간 동안의 활동을 보여줄 수 있는 기록을 남기기 위함
- 반응형 필수(PC·태블릿·모바일에서 깨지지 않아야 함)
- 사이트에서 내세울 정체성: **iOS 개발자**(Swift · SwiftUI · UIKit · Xcode)

## 브랜치
- Git Flow를 따른다. `master`(배포) ← `develop`(통합) ← `feature/*`(작업)
- `feature/design-theme-dock`: 라이트/다크 모드 + Dock 자동 숨김 (완료, `develop` 미병합)
- `feature/design-color-direction`: 색상 방향 후보 비교 → G 확정 → 4개 화면 적용 (완료, `feature/design-theme-dock`에서 분기, `develop` 미병합)
- `feature/design-theme-toggle`: 홈 메뉴바 테마 전환 버튼 (완료, `feature/design-color-direction`에서 분기, `develop` 미병합)
- `feature/design-detail-screens`: 스터디 글 상세·행사 상세 시안 (현재 작업 브랜치, `feature/design-theme-toggle`에서 분기, `develop` 미병합)

## 확정된 결정
- 디자인 방향: **데스크톱 OS 메타포**. 상단 유리 메뉴바가 네비게이션, 각 페이지는 창 안에 열리고, 하단에 Dock
- 화면 구성: 홈 / 프로젝트 / 스터디 기록 / 세미나 기록 4개
- 홈 구성: `About me` 창 + 코드 에디터 창(`ProfileView.swift`) + iPhone 목업, 스크롤하면 `최근 기록` 창
- 홈에는 프로젝트 그리드를 두지 않는다(프로젝트 수가 아직 적음). 프로젝트는 전용 페이지에서만 보여준다
- "이력서" 버튼, "프로젝트 보기" 버튼은 넣지 않는다. 스크롤 힌트로 대체
- 히어로 문구: 인사말 `안녕하세요, iOS 개발자 Chu Yumin입니다.`(44px)가 상태 메시지보다 한 단계 크다
- 좋아하는 문구(확정): `기회가 주어지면 최선을 다하는 것이 아니라 최선을 다하고 있으면 기회가 주어지는 것이다.`
- 영문 이름 표기는 항상 `Chu Yumin`
- **라이트/다크 모드 (2026-09-13 확정)**: 바탕만 전환한다. 데스크톱 바탕·글로우, 메뉴바·Dock 유리, 바탕 위 글자(메뉴·바로가기 라벨·스크롤 힌트)만 바뀌고, 창 본문은 두 모드 모두 밝게, 코드 에디터 창은 항상 어둡게 유지
- **테마 전환 버튼 (2026-09-13 추가)**: 홈 메뉴바 오른쪽, 검색 아이콘 왼쪽에 28×22 버튼. 직접 그린 SVG로 누르면 바뀔 모드의 아이콘을 보여줌(다크=해, 라이트=달), `aria-label`은 "라이트/다크 모드로 전환". 실제 구현 시에는 모든 화면의 메뉴바에 두고, 첫 방문은 `prefers-color-scheme`을 따르며 선택값은 `localStorage`에 저장한다. 모바일에서는 누르는 영역을 44px 이상으로 키운다
- **상세 화면 (2026-09-13 추가, 정적 시안)**: 목록 항목을 누르면 같은 창 안에서 상세로 바뀌고 상단 `← 목록 이름` 링크로 돌아간다. 메뉴바·Dock 활성 표시는 목록과 같게 유지
  - 스터디 글 상세: 카테고리·제목(36px)·날짜·읽는 시간 → 본문(17px / 줄 간격 1.85, 소제목 24px, 인라인 코드, Swift 코드 블록) + 오른쪽 목차(240px, 현재 위치는 라일락 선) → 이전/다음 글 카드
  - 행사 상세는 **글 흐름형으로 확정**(2026-09-13). 사용자가 "세미나 내용이 길 수 있으니 글 몇 줄에 사진 한두 장씩 넣는 레이아웃"을 요청해 사진 묶음형(A)과 비교한 뒤 선택. A안은 커밋 `1769266`에 남아 있다
    - 구성: 읽기 폭 800px. 날짜·장소·제목 → 창 폭 표지 사진 → 도입 문단 → 세션별 섹션(`SESSION 01 · [발표자]` + 제목 + 문단 사이 사진) → 배운 점·소감 → 이전/다음 행사
    - 사진 배치 3종을 세션마다 골라 쓴다: 넓게 1장(450px) / 2장 나란히(290px) / 사진 옆에 글(340px 사진 + 글, 모바일은 위아래로 쌓음). 사진마다 설명(`figcaption`) 자리
- **Dock 자동 숨김 (2026-09-13 확정)**: 처음에는 보임 → 아래로 스크롤하면 숨김 → 위로 스크롤하거나 마우스가 화면 하단 영역에 들어오면 나타남. 스크롤로 숨겨진 상태에서 마우스로 불러냈다면 영역을 벗어날 때 다시 숨김. 숨은 동안 하단 중앙에 얇은 힌트 막대를 남김. 모바일은 hover가 없으므로 스크롤 규칙만 적용
- **색상 방향 (2026-09-13 확정): G · 라일락 & 민트**. 짙은 자두색 바탕 + 라일락(주 액센트: 링크·태그·활성 표시) + 민트(보조 액센트: 아이콘·코드 강조). 7개 후보(인디고·그래파이트·샌드·포레스트·앰버 코랄·티타늄 마젠타·라일락&민트)를 비교한 뒤 사용자가 "iOS 개발자라는 인식과 강한 개성에 도움이 된다"며 선택. 기술적 무게감이 가벼운 약점은 코드 에디터 창(항상 어두움)·iPhone 목업 같은 개발자 요소로 보완한다. 비교했던 A~F 시안은 커밋 `de7d440`에 남아 있다
- 기술 스택: **보류**(시안 마무리 후 결정). 후보는 아래 "다음에 할 일" 참고

## 저작권 주의선
macOS·Xcode의 실제 UI는 복제하지 않는다. Apple 로고, SF Symbols, 신호등 색(빨강·노랑·초록) 창 컨트롤, 실제 메뉴 구조를 쓰지 않고 **자체 팔레트와 직접 그린 SVG 아이콘**으로 대체한다. 창 컨트롤 점도 라일락 2개 + 민트 1개로 자체 색을 쓴다. 이 선은 계속 유지한다.

## 디자인 시스템 현황 (G · 라일락 & 민트)
- 폰트: Noto Sans KR (300/400/500/700/800/900), 코드 영역은 `ui-monospace` 계열
- 중립색: 모두 색상각 290(자두색 기운)으로 통일. 본문 글자 `oklch(18% 0.015 290)`, 보조 글자 `oklch(46% 0.015 290)` 안팎
- 창 본문 `oklch(98% 0.006 290)` / 창 타이틀 바 `oklch(94% 0.006 290)` (모드와 무관)
- 라일락 주 액센트 `oklch(60% 0.15 295)` (호버 `oklch(50% 0.15 295)`), 태그 바탕 `oklch(95% 0.03 295)` / 태그 글자 `oklch(46% 0.14 295)`
- 창 컨트롤 점: 밝은 창 `oklch(72% 0.06 295)` · `oklch(81% 0.04 295)` · 민트 `oklch(86% 0.06 170)`
- 앱 아이콘 그라디언트(메뉴바 로고·바로가기·Dock 공통): 홈 라일락 `70% 0.13 300 → 58% 0.15 285` / 프로젝트 민트 `76% 0.11 170 → 64% 0.11 185` / 스터디 핑크 `72% 0.12 340 → 60% 0.14 320` / 세미나 하늘 `78% 0.1 220 → 64% 0.12 255` / 메일 민트하늘 `74% 0.1 195 → 62% 0.11 215` / 터미널 아이콘 선 민트 `84% 0.11 170`
- 코드 에디터: 바탕 `oklch(19% 0.018 290)`, 문법색 키워드 라일락 `76% 0.13 300` / 타입 민트 `80% 0.11 170` / 메서드·숫자 하늘 `80% 0.1 220` / 문자열 버터 `82% 0.1 80`
- 그 밖의 유채색(프로젝트 썸네일·세미나 사진 자리·iPhone 화면 등)은 색상각 구간 규칙으로 옮김: 190~235 → 민트 175, 236~285 → 라일락 295, 286~320 → 핑크 335 (채도는 0.75~0.8배)
- 유리 재질(메뉴바·Dock 공통): `var(--glass)` + `blur(28px) saturate(180%)` + 상단 하이라이트 `inset 0 1px 0 var(--glass-hi)` + 그림자
- 테마 토큰: 각 `.dc.html`의 `<helmet><style>`에서 `.site`(다크 기본)와 `.site[data-theme="light"]`에 CSS 변수로 정의. 실제 코드로 옮길 때 이 표를 그대로 테마 토큰으로 쓴다

| 토큰 | 용도 | 다크 | 라이트 |
| --- | --- | --- | --- |
| `--desk` | 데스크톱 바탕 | `oklch(18% 0.02 290)` | `oklch(96% 0.012 290)` |
| `--glow-a/b/c` | 블러 글로우 원 | `58% 0.13 300` / 민트 `60% 0.1 170` / `52% 0.12 300` | `86% 0.08 300` / 민트 `89% 0.07 170` / `90% 0.06 300` |
| `--glass` | 메뉴바·Dock 유리 | 흰색 `/ 0.10` | 흰색 `/ 0.55` |
| `--glass-line` | 유리 테두리 | 흰색 `/ 0.12` | `oklch(25% 0.022 290 / 0.08)` |
| `--glass-hi` | 유리 상단 하이라이트 | 흰색 `/ 0.18` | 흰색 `/ 0.7` |
| `--glass-shadow` | 메뉴바 그림자 | `oklch(10% 0.022 290 / 0.35)` | `oklch(40% 0.024 295 / 0.10)` |
| `--ink-strong` | 메뉴바 이름·활성 메뉴 | `oklch(97% 0.006 290)` | `oklch(20% 0.015 290)` |
| `--ink` | 메뉴바 시계·검색 아이콘 | `oklch(88% 0.015 290)` | `oklch(32% 0.015 290)` |
| `--ink-muted` | 비활성 메뉴 | `oklch(82% 0.015 290)` | `oklch(42% 0.015 290)` |
| `--menu-active` | 활성 메뉴 배경 | 흰색 `/ 0.14` | `oklch(25% 0.022 290 / 0.08)` |
| `--label` | 바로가기 아이콘 라벨 | `oklch(90% 0.012 290)` | `oklch(28% 0.015 290)` |
| `--cue` | 스크롤 힌트 | `oklch(76% 0.022 290)` | `oklch(46% 0.022 290)` |
| `--dock-sep` | Dock 구분선·숨김 힌트 막대 | 흰색 `/ 0.16` | `oklch(25% 0.022 290 / 0.14)` |
| `--dock-dot` | Dock 실행 중 점 | `oklch(90% 0.015 290)` | `oklch(35% 0.015 290)` |
| `--dock-shadow` | Dock 그림자 | `oklch(8% 0.022 290 / 0.5)` | `oklch(40% 0.024 295 / 0.16)` |
| `--win-line` | 창 테두리 | 흰색 `/ 0.12` | `oklch(25% 0.022 290 / 0.08)` |
| `--win-shadow` | 창·iPhone 그림자 | `oklch(6% 0.022 290 / 0.55)` | `oklch(35% 0.024 295 / 0.18)` |
| `--icon-shadow` | 바로가기 아이콘 그림자 | `oklch(8% 0.022 290 / 0.45)` | `oklch(40% 0.024 295 / 0.22)` |

- Dock 숨김 모션: `transform .38s cubic-bezier(.2,.8,.2,1)` + `opacity .28s ease`, 숨을 때 `translateY(calc(100% + 32px))`. 테마 전환 시 바탕은 `background-color .4s ease`

## 시안 캔버스
- URL: https://claude.ai/code/artifact/48a3c34c-b882-4f13-8e2f-7e3668bdb7b1
- v1: 일반 웹페이지 4장 → v2: iOS 정체성 강화(iPhone 목업, 이력서 제거) → v3: 데스크톱 메타포 → v4: 라이트/다크 + Dock 자동 숨김 → v5~v6: 색상 방향 후보 A~G 비교 → v7: G · 라일락 & 민트 적용 → v8: 홈 메뉴바 테마 전환 버튼 → v9: 스터디 글 상세·행사 상세 화면 → v10: 행사 상세 B 글 흐름형 추가 → **v11: 행사 상세를 글 흐름형으로 확정(현재)**
- 페이지 2개: `시안`(4개 화면 + 테마·Dock 규칙 메모, 캔버스를 열면 홈 화면부터 보임) / `색상 방향`(G 팔레트 기준표 1장 + 결정 메모)
- 각 화면 위의 조절 칩: `theme`(dark / light). 홈에만 `dockPreview`(auto = 실제 동작 / shown / hidden)

## 작업 파일
- `design/Main.dc.html` — 홈(데스크톱 화면). 좌측 바로가기 아이콘 4개, About 창, 코드 에디터 창, iPhone 목업, Dock(`.dockzone` 하단 112px 영역 + 자동 숨김 로직), 최근 기록 창
- `design/Projects.dc.html` — 프로젝트 창(카드 6개, iPhone 프레임 썸네일 + 앱 아이콘 스퀘어클, 필터 칩)
- `design/StudyLog.dc.html` — 스터디 기록 창(포스트 6개, 카테고리 필터)
- `design/Seminars.dc.html` — 세미나 기록 창(타임라인 5개, 사진 자리)
- `design/StudyPost.dc.html` — 스터디 글 상세(본문·코드 블록·목차·이전/다음 글). 캔버스에서 스터디 기록 목록 바로 아래
- `design/SeminarDetail.dc.html` — 행사 상세, 글 흐름형(표지 사진·세션별 문단 사이 사진·소감·이전/다음 행사). 캔버스에서 세미나 기록 목록 바로 아래
- `design/DirectionG.dc.html` — G 팔레트 기준표(다크/라이트 미니 홈 화면 + 색 견본)
- 서브 화면 3개의 Dock은 아직 페이지 하단에 놓인 정적 블록이다. 실제 구현에서는 모든 화면에서 홈과 같은 고정 + 자동 숨김 규칙을 쓴다
- 테마 토큰 CSS는 4개 파일에 똑같이 복제되어 있다(캔버스 아트보드끼리는 CSS를 공유할 수 없음). 토큰 값을 바꿀 때는 4개 파일을 함께 수정한다
- `design/canvas.json` — 페이지·아트보드 배치·크기와 메모. 홈 1440×1640 / 프로젝트 1440×1480 / 스터디 1440×1760 / 세미나 1440×2620 / G 기준표 1232×720
- `design/chu-yumin-portfolio.html` — 위 파일들을 조립한 결과물(직접 편집하지 말 것, 항상 재조립)
- `스크린샷 2026-09-11 오후 11.52.51.png` — 참고한 데스크톱형 포트폴리오 사례

## 시안을 수정하고 다시 올리는 방법
`design/` 폴더에서 작업한다. 스킬 경로는 세션마다 달라질 수 있으므로, 달라졌다면 `/design`을 다시 실행해 경로를 확인한다.

1. 새 세션이라면 먼저 캔버스를 읽어(`Artifact` read) GUI에서 저장된 변경이 있는지 확인한다. `seed-canvas.mjs --extract <저장된 파일> --to <빈 폴더>`로 꺼내 `design/` 파일과 비교하고, 다르면 추출본을 기준으로 작업한다
2. `design/*.dc.html` 또는 `canvas.json`을 수정한다
3. 재조립: `node "<design 스킬 경로>/seed-canvas.mjs" --template "<design 스킬 경로>/payload.template.html" --out chu-yumin-portfolio.html --title "Chu Yumin Portfolio" --artboard Main.dc.html --artboard Projects.dc.html --artboard StudyLog.dc.html --artboard Seminars.dc.html --artboard StudyPost.dc.html --artboard SeminarDetail.dc.html --artboard DirectionG.dc.html --canvas canvas.json`
4. 검증: 같은 스크립트에 `--check chu-yumin-portfolio.html`
5. Artifact 재발행. 새 세션에서는 반드시 위 URL을 `url`로 넘긴다(안 넘기면 새 캔버스가 생김). `contract: 0.1.31`, favicon `💻`, `capabilities`는 생략

## 다음에 할 일
- [ ] 프로젝트 상세 화면이 필요한지 확인(필요하면 같은 흐름으로 시안 추가)
- (G 적용 결과는 사용자가 확인 후 "만족"으로 마무리, 2026-09-13)
- [ ] 실제 모션 설계(스크롤 등장, 창 포커스 전환). 현재 시안에는 스크롤 힌트, Dock 숨김, 테마 전환 트랜지션만 있음
- [ ] 반응형 설계(모바일에서 창·Dock·메뉴바를 어떻게 접을지)
- [ ] iPhone 목업을 Apple Design Resources 이미지로 교체. 캔버스는 외부 이미지를 못 불러오므로 **로컬 파일**을 받아 `--image` 옵션으로 포함해야 한다(사용 조건은 사용자가 확인)
- [ ] 기술 스택 확정(보류 중). 추천은 **Astro + Tailwind CSS**(콘텐츠 중심, 이미지 최적화 내장, 필요한 부분만 island). 창 전환 같은 동적 인터랙션이 많아지면 Next.js
- [ ] 대괄호 placeholder를 실제 내용으로 교체 — 프로젝트 이름·소개·태그, 포스트 제목·요약·날짜, 세미나 이름·장소·소감, 연락처 링크(GitHub/Email/LinkedIn)
- [ ] `feature/design-theme-dock`, `feature/design-color-direction`을 `develop`에 병합(사용자 확인 후)

## 문서 관리 규칙
- 이 문서(`PROGRESS.md`)가 현재 상태의 단일 기준이다. 작업이 끝나면 항상 최신화한다
- 완료된 항목이 쌓이면 "다음에 할 일"에서 지우고 "확정된 결정"으로 옮긴다
