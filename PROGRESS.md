# 진행 상황
최종 업데이트: 2026-09-13

## 현재 상태 한 줄 요약
디자인 시안(4개 화면)은 **데스크톱 메타포로 확정**되었고, **라이트/다크 모드(바탕만 전환)**와 **Dock 자동 숨김 동작**이 시안에 들어갔다. 남은 시안 작업은 색상·디테일 조정과 모션 설계이며, 기술 스택은 시안을 마무리한 뒤 정하기로 보류했다. 아직 사이트 코드는 작성하지 않았다.

## 프로젝트 개요
- 자기소개, 프로젝트 포트폴리오, 학습 기록, 세미나·행사 기록(사진 포함)을 모으는 개인 홈페이지
- 만드는 이유: 회사에 소속되지 않은 기간 동안의 활동을 보여줄 수 있는 기록을 남기기 위함
- 반응형 필수(PC·태블릿·모바일에서 깨지지 않아야 함)
- 사이트에서 내세울 정체성: **iOS 개발자**(Swift · SwiftUI · UIKit · Xcode)

## 브랜치
- Git Flow를 따른다. `master`(배포) ← `develop`(통합) ← `feature/*`(작업)
- 현재 작업 브랜치: `feature/design-theme-dock` (라이트/다크 모드 + Dock 자동 숨김 시안)

## 확정된 결정
- 디자인 방향: **데스크톱 OS 메타포**. 상단 유리 메뉴바가 네비게이션, 각 페이지는 창 안에 열리고, 하단에 Dock
- 화면 구성: 홈 / 프로젝트 / 스터디 기록 / 세미나 기록 4개
- 홈 구성: `About me` 창 + 코드 에디터 창(`ProfileView.swift`) + iPhone 목업, 스크롤하면 `최근 기록` 창
- 홈에는 프로젝트 그리드를 두지 않는다(프로젝트 수가 아직 적음). 프로젝트는 전용 페이지에서만 보여준다
- "이력서" 버튼, "프로젝트 보기" 버튼은 넣지 않는다. 스크롤 힌트로 대체
- 히어로 문구: 인사말 `안녕하세요, iOS 개발자 Chu Yumin입니다.`(44px)가 상태 메시지보다 한 단계 크다
- 좋아하는 문구(확정): `기회가 주어지면 최선을 다하는 것이 아니라 최선을 다하고 있으면 기회가 주어지는 것이다.`
- 영문 이름 표기는 항상 `Chu Yumin`
- **라이트/다크 모드 (2026-09-13 확정)**: 바탕만 전환한다. 데스크톱 바탕·글로우, 메뉴바·Dock 유리, 바탕 위 글자(메뉴·바로가기 라벨·스크롤 힌트)만 바뀌고, 창 본문은 두 모드 모두 밝게, 코드 에디터 창은 항상 어둡게 유지
- **Dock 자동 숨김 (2026-09-13 확정)**: 처음에는 보임 → 아래로 스크롤하면 숨김 → 위로 스크롤하거나 마우스가 화면 하단 영역에 들어오면 나타남. 스크롤로 숨겨진 상태에서 마우스로 불러냈다면 영역을 벗어날 때 다시 숨김. 숨은 동안 하단 중앙에 얇은 힌트 막대를 남김. 모바일은 hover가 없으므로 스크롤 규칙만 적용
- 기술 스택: **보류**(시안 마무리 후 결정). 후보는 아래 "다음에 할 일" 참고
- **색상 방향 (2026-09-13 확정): G · 라일락 & 민트**. 짙은 자두색 바탕 + 라일락(주 액센트: 링크·태그·활성 표시) + 민트(보조 액센트: 아이콘·코드 강조 등). 사용자가 고른 이유: iOS 개발자라는 인식과 강한 개성에 도움이 된다고 봄. 기술적 무게감이 가벼운 약점은 코드 에디터 창(항상 어두움)·iPhone 목업 같은 개발자 요소로 보완한다

## 저작권 주의선
macOS·Xcode의 실제 UI는 복제하지 않는다. Apple 로고, SF Symbols, 신호등 색(빨강·노랑·초록) 창 컨트롤, 실제 메뉴 구조를 쓰지 않고 **자체 팔레트와 직접 그린 SVG 아이콘**으로 대체한다. 이 선은 계속 유지한다.

## 디자인 시스템 현황
- 창 본문: `oklch(98% 0.004 260)` / 창 타이틀 바: `oklch(94% 0.004 260)` (모드와 무관)
- 액센트: `oklch(62% 0.19 262)` (호버 `oklch(52% 0.19 262)`)
- 폰트: Noto Sans KR (300/400/500/700/800/900), 코드 영역은 `ui-monospace` 계열
- 유리 재질(메뉴바·Dock 공통): `var(--glass)` + `blur(28px) saturate(180%)` + 상단 하이라이트 `inset 0 1px 0 var(--glass-hi)` + 그림자
- 테마 토큰: 각 `.dc.html`의 `<helmet><style>`에서 `.site`(다크 기본)와 `.site[data-theme="light"]`에 CSS 변수로 정의. 실제 코드로 옮길 때 이 표를 그대로 테마 토큰으로 쓴다

| 토큰 | 용도 | 다크 | 라이트 |
| --- | --- | --- | --- |
| `--desk` | 데스크톱 바탕 | `oklch(15% 0.012 260)` | `oklch(94% 0.01 262)` |
| `--glow-a/b/c` | 블러 글로우 원 | `55% 0.2 262` / `45% 0.18 305` / `48% 0.16 275` | `76% 0.12 262` / `80% 0.1 305` / `82% 0.09 275` |
| `--glass` | 메뉴바·Dock 유리 | 흰색 `/ 0.10` | 흰색 `/ 0.55` |
| `--glass-line` | 유리 테두리 | 흰색 `/ 0.12` | `oklch(25% 0.02 265 / 0.08)` |
| `--glass-hi` | 유리 상단 하이라이트 | 흰색 `/ 0.18` | 흰색 `/ 0.7` |
| `--glass-shadow` | 메뉴바 그림자 | `oklch(10% 0.02 265 / 0.35)` | `oklch(40% 0.03 265 / 0.10)` |
| `--ink-strong` | 메뉴바 이름·활성 메뉴 | `oklch(97% 0.004 260)` | `oklch(20% 0.01 260)` |
| `--ink` | 메뉴바 시계·검색 아이콘 | `oklch(88% 0.01 260)` | `oklch(32% 0.01 260)` |
| `--ink-muted` | 비활성 메뉴 | `oklch(82% 0.01 260)` | `oklch(42% 0.01 260)` |
| `--menu-active` | 활성 메뉴 배경 | 흰색 `/ 0.14` | `oklch(25% 0.02 265 / 0.08)` |
| `--label` | 바로가기 아이콘 라벨 | `oklch(90% 0.008 260)` | `oklch(28% 0.01 260)` |
| `--cue` | 스크롤 힌트 | `oklch(76% 0.015 260)` | `oklch(46% 0.015 260)` |
| `--dock-sep` | Dock 구분선·숨김 힌트 막대 | 흰색 `/ 0.16` | `oklch(25% 0.02 265 / 0.14)` |
| `--dock-dot` | Dock 실행 중 점 | `oklch(90% 0.01 260)` | `oklch(35% 0.01 260)` |
| `--dock-shadow` | Dock 그림자 | `oklch(8% 0.02 265 / 0.5)` | `oklch(40% 0.03 265 / 0.16)` |
| `--win-line` | 창 테두리 | 흰색 `/ 0.12` | `oklch(25% 0.02 265 / 0.08)` |
| `--win-shadow` | 창·iPhone 그림자 | `oklch(6% 0.02 265 / 0.55)` | `oklch(35% 0.03 265 / 0.18)` |
| `--icon-shadow` | 바로가기 아이콘 그림자 | `oklch(8% 0.02 265 / 0.45)` | `oklch(40% 0.03 265 / 0.22)` |

- Dock 숨김 모션: `transform .38s cubic-bezier(.2,.8,.2,1)` + `opacity .28s ease`, 숨을 때 `translateY(calc(100% + 32px))`. 테마 전환 시 바탕은 `background-color .4s ease`

## 시안 캔버스
- URL: https://claude.ai/code/artifact/48a3c34c-b882-4f13-8e2f-7e3668bdb7b1
- v1: 일반 웹페이지 4장(기본 구조) → v2: iOS 정체성 강화(iPhone 목업, 이력서 제거) → v3: 데스크톱 메타포 전면 적용 → **v4: 라이트/다크 모드 토글 + Dock 자동 숨김(현재)**
- 각 화면 위의 조절 칩: `theme`(dark / light). 홈에만 `dockPreview`(auto = 실제 동작 / shown / hidden)
- 캔버스 홈 화면 아래에 테마 규칙·Dock 규칙 메모 2개가 붙어 있다

## 작업 파일
- `design/Main.dc.html` — 홈(데스크톱 화면). 좌측 바로가기 아이콘 4개, About 창, 코드 에디터 창, iPhone 목업, Dock(`.dockzone` 하단 112px 영역 + 자동 숨김 로직), 최근 기록 창
- `design/Projects.dc.html` — 프로젝트 창(카드 6개, iPhone 프레임 썸네일 + 앱 아이콘 스퀘어클, 필터 칩)
- `design/StudyLog.dc.html` — 스터디 기록 창(포스트 6개, 카테고리 필터)
- `design/Seminars.dc.html` — 세미나 기록 창(타임라인 5개, 사진 자리)
- 서브 화면 3개의 Dock은 아직 페이지 하단에 놓인 정적 블록이다. 실제 구현에서는 모든 화면에서 홈과 같은 고정 + 자동 숨김 규칙을 쓴다
- 테마 토큰 CSS는 4개 파일에 똑같이 복제되어 있다(캔버스 아트보드끼리는 CSS를 공유할 수 없음). 토큰 값을 바꿀 때는 4개 파일을 함께 수정한다
- `design/canvas.json` — 아트보드 배치·크기와 메모. 홈 1440×1640 / 프로젝트 1440×1480 / 스터디 1440×1760 / 세미나 1440×2620
- `design/chu-yumin-portfolio.html` — 위 파일들을 조립한 결과물(직접 편집하지 말 것, 항상 재조립)
- `스크린샷 2026-09-11 오후 11.52.51.png` — 참고한 데스크톱형 포트폴리오 사례

## 시안을 수정하고 다시 올리는 방법
`design/` 폴더에서 작업한다. 스킬 경로는 세션마다 달라질 수 있으므로, 달라졌다면 `/design`을 다시 실행해 경로를 확인한다.

1. 새 세션이라면 먼저 캔버스를 읽어(`Artifact` read) GUI에서 저장된 변경이 있는지 확인한다. `seed-canvas.mjs --extract <저장된 파일> --to <빈 폴더>`로 꺼내 `design/` 파일과 비교하고, 다르면 추출본을 기준으로 작업한다
2. `design/*.dc.html` 또는 `canvas.json`을 수정한다
3. 재조립: `node "<design 스킬 경로>/seed-canvas.mjs" --template "<design 스킬 경로>/payload.template.html" --out chu-yumin-portfolio.html --title "Chu Yumin Portfolio" --artboard Main.dc.html --artboard Projects.dc.html --artboard StudyLog.dc.html --artboard Seminars.dc.html --canvas canvas.json`
4. 검증: 같은 스크립트에 `--check chu-yumin-portfolio.html`
5. Artifact 재발행. 새 세션에서는 반드시 위 URL을 `url`로 넘긴다(안 넘기면 새 캔버스가 생김). `contract: 0.1.31`, favicon `💻`, `capabilities`는 생략

## 진행 중: 색상 방향 고르기 → G · 라일락 & 민트로 결정, 4개 화면 반영 대기
- G 팔레트 원본: `design/DirectionG.dc.html` (바탕 다크 `oklch(18% 0.02 290)` / 라이트 `oklch(96% 0.012 290)`, 액센트 `oklch(60% 0.15 295)`, 태그 `oklch(95% 0.03 295)`·`oklch(46% 0.14 295)`, 창 본문 `oklch(98.5% 0.005 290)`, 코드 창 `oklch(19% 0.02 290)`, 민트 `oklch(76% 0.11 170)` 계열)
- 브랜치: `feature/design-color-direction` (`feature/design-theme-dock`에서 분기, 두 브랜치 모두 아직 `develop`에 병합하지 않음)
- 캔버스를 페이지 2개로 나눔: `시안`(기존 4화면) / `색상 방향`(후보 4개, 캔버스를 열면 이 페이지부터 보임)
- 후보는 구조·타이포·크기가 모두 같고 색만 다르며, 각각 다크/라이트 바탕 미니 화면과 색 견본을 보여준다
  - A · 미드나이트 인디고(현재): 파랑·보라 글로우. 대신 흔한 테크 사이트 인상
  - B · 그래파이트: 무채색 + 청록 액센트 하나, 글로우 거의 없음. 대신 심심할 수 있고 Dock 아이콘 구분이 약함
  - C · 샌드: 따뜻한 모래 바탕 + 테라코타. 대신 기술적 인상이 옅고 다크 모드가 갈색 톤
  - D · 포레스트: 짙은 초록 + 민트. 대신 초록이 성공·완료 색과 겹침
  - 사용자 요청("iOS 개발자 인상을 남기면서 개성을 더하고 싶다", 2026-09-13)으로 E~G 추가
  - E · 앰버 코랄: 남색 바탕 + 코랄 오렌지(Swift를 떠올리게 하는 주황을 로고 없이 색으로만). 대신 주황은 아껴 써야 하고 경고색처럼 보이지 않게 채도 조절 필요
  - F · 티타늄 마젠타: 금속성 회색 바탕 + 마젠타. 대신 호불호가 갈리고 라이트 모드가 차갑게 느껴질 수 있음
  - G · 라일락 & 민트: 짙은 자두색 바탕 + 액센트 2개(라일락은 링크·태그, 민트는 보조). 대신 쓰임새 규칙이 필요하고 기술적 무게감이 가장 가벼움
- 작업 파일: `design/DirectionA~G.dc.html`. 스크립트로 한 번에 생성했지만, 이후 수정은 파일을 직접 고친다
- 사용자가 방향을 고르면(또는 섞을 조합을 말하면) 그 팔레트를 4개 화면의 토큰과 창·태그·아이콘 색에 반영한다. 고르지 않은 후보는 삭제하거나 `색상 방향` 페이지에 보관한다
- 재조립할 때는 `--artboard DirectionA.dc.html` ~ `DirectionG.dc.html` 7개를 추가로 넘겨야 한다

## 다음에 할 일
- [ ] G · 라일락 & 민트를 4개 화면(토큰, 창·태그·링크·Dock 아이콘·코드 창·iPhone 목업)에 반영, 라이트 모드 글로우 강도 조정, 고르지 않은 후보 A~F 정리
- [ ] 실제 모션 설계(스크롤 등장, 창 포커스 전환). 현재 시안에는 스크롤 힌트, Dock 숨김, 테마 전환 트랜지션만 있음
- [ ] 반응형 설계(모바일에서 창·Dock·메뉴바를 어떻게 접을지)
- [ ] iPhone 목업을 Apple Design Resources 이미지로 교체. 캔버스는 외부 이미지를 못 불러오므로 **로컬 파일**을 받아 `--image` 옵션으로 포함해야 한다(사용 조건은 사용자가 확인)
- [ ] 기술 스택 확정(보류 중). 추천은 **Astro + Tailwind CSS**(콘텐츠 중심, 이미지 최적화 내장, 필요한 부분만 island). 창 전환 같은 동적 인터랙션이 많아지면 Next.js
- [ ] 대괄호 placeholder를 실제 내용으로 교체 — 프로젝트 이름·소개·태그, 포스트 제목·요약·날짜, 세미나 이름·장소·소감, 연락처 링크(GitHub/Email/LinkedIn)

## 문서 관리 규칙
- 이 문서(`PROGRESS.md`)가 현재 상태의 단일 기준이다. 작업이 끝나면 항상 최신화한다
- 완료된 항목이 쌓이면 "다음에 할 일"에서 지우고 "확정된 결정"으로 옮긴다
